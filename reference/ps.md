# Parry Sound `sf` object

A `MULTIPOLYGON` `sf` object that is Parry Sound which is a large
embayment on Georgian Bay, Lake Huron

## Usage

``` r
ps
```

## Format

A `sf` object with a single geomety feature. Current coordinate
reference system is WGS 84.
