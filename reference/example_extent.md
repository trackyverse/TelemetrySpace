# Example array extent

Example array extent used in each model

## Usage

``` r
example_extent
```

## Format

A data frame with two variables and two row: `ylim` is the minimum and
maximum extent on the y-axis (i.e., latitudinal) for the array and
`xlim` is the minimum and maximum extent on the x-axis (i.e.,
longitudinal) for the array.
