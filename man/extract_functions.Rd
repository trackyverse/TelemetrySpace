% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{extract_functions}
\alias{extract_functions}
\alias{extract_coa}
\alias{extract_loc_draws}
\alias{extract_param_draws}
\title{Extract Draws}
\usage{
extract_coa(summary_draws)

extract_loc_draws(draws)

extract_param_draws(draws)
}
\arguments{
\item{summary_draws}{a \code{draws_summary} object from \code{posterior::summarize_draws()}}

\item{draws}{a \code{draws_df} object from \code{posterior::as_draws_df()}}
}
\value{
\code{extract_coa()} - returns a \code{data.frame} containing
the median and the 2.5, and 97.5\% quantiles.

\code{extract_loc_draws()} - returns a \code{data.frame} containing the following columns:
\code{.chain}, \code{.iteration}, \code{.draw}, \code{lp__}, \code{fish}, \code{time}, \code{x}, and \code{y}.

\code{extract_param_draws()} - returns a \code{data.frame} containing the following columns:
\code{.chain}, \code{.iteration}, \code{.draw}, \code{lp__} and then posterior draws for the paramaters of
the detection probablity likihood (i.e., \code{alpha0} and \code{alpha1}) and generated quantiteies (i.e., \code{p0}).
}
\description{
These functions allow draws to be extracted from \code{{posterior}} objects.
}
\details{
\code{extract_coa()} - extracts median and the 2.5, and 97.5\% quantiles for posterior draws of
\code{sx} and \code{sy}, which is the estiamted center of activity for a given individual within
a given time bin.

\code{extract_loc_draws()} - extracts posterior draws for the latent variables \code{sx} and \verb{sy`` for each fish at each time bin from }draws_df\verb{object and transforms it so that the fish number, time, and draw are in a}data.frame\verb{. This can then be further ploted or transformed into a }sf` object.

\code{extract_param_draws()} - extracts posterior draws for the detection intercept (i.e., logit scale; \code{alpha0}),
the distance-decay coefficient (i.e., \code{alpha1}), any other coefficents, and the detction probablity at distance 0
(i.e. \code{p0}) from  \code{draws_df} object.
}
\keyword{internal}
